<li>
	{{burger_name}}

	{{#unless devoured}}

	<button class="change-devour" type= "submit" data-id="{{id}}" data-newdevoured="{{true}}">
		{{!-- {{#if devoured}} --}}  DEVOUR IT!!!!  {{!-- {{else}} EAT TIME!{{/if}} --}}
	</button>

	{{/unless}}

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
